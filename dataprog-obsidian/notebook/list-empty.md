---
jupytext:
  text_representation:
    extension: .md
    format_name: myst
    format_version: 0.13
    jupytext_version: 1.17.0
kernelspec:
  display_name: Python 3 (ipykernel)
  language: python
  name: python3
---

# Oppgåver til Demo Listekomprehensjon

Desse oppgåvene er utgangspunktet for ein demonstrasjonsvideo med to 
formål.  Eg skal demonstrera suntaksen for listekomprehensjon i 
python, og eg skal vise litt korleis eg tenkjer når eg tek fatt på å skriva
kode.

::: {admonition} Oppgåve
Lag ei liste med dei ti fyrste kvadrattala, dvs. tal som
kan skrivast som $x^2$ for eit heiltal $x$.
:::

```{code-cell} ipython3
## TODO
```

```{code-cell} ipython3
def f(x):
    return x**2 - 2*x + 3
```

::: {admonition} Oppgåve
Lag ei liste med verdiane av $f(x)$ for $x=0,1,2,\ldots,9$,
der $f(x)$ er definert som `f` over.
:::

```{code-cell} ipython3
## TODO
```

::: {admonition} Oppgåve
Lag ei liste med par $x, f(x)$ for $x=0,1,2,\ldots,9$.
:::

```{code-cell} ipython3
## TODO
```

::: {admonition} Oppgåve
Lag eit program som tek ei ei liste med strengar og
lagar ei ny liste der tomme strengar (`""`) er fjerna.
:::

```{code-cell} ipython3
## TODO
```

::: {admonition} Oppgåve
Lag eit program som lager den lille gangetabellen som ei
liste av liste.  
Dvs. kvar rad er ei liste av produkt $x\cdot y$ der $y=0,1,\ldots,10$,
og tabellen er ei liste med radar for $x=0,1,\ldots,10$.
:::

```{code-cell} ipython3
## TODO
```

